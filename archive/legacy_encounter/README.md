# Legacy Encounter Prototype

This folder archives the early four-unit text encounter prototype.

It is kept as reference material only. The current 1.0 path is the mirrored 3v3 win-rate evidence chain:

```text
natural language -> LLM policy -> mirror_map_sim -> batch win-rate stats
```

The archived files may require path fixes if someone wants to run them again.
